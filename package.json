{
    "name": "app",
    "version": "1.0.0",
    "scripts": {
      "build": "pipenv install && pipenv run python app.py"
    },
    "dependencies": {
      "express": "^4.17.1",
      "flask": "*",
      "pipenv": "*",
      "werkzeug": "*"
    }
}